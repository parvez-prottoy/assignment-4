import React from "react";

const Button = () => {
  return (
    <button
      type="submit"
      className="inline-block shrink-0 rounded-md border border-blue-600 bg-cls-1 px-12 py-3 text-sm font-bold text-white transition hover:bg-transparent hover:text-blue-600 focus:outline-none focus:ring active:text-cls-1"
    >
      Submit
    </button>
  );
};

export default Button;
